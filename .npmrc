# O Electron baixa o binario do aplicativo num script de pos-instalacao.
# Se os scripts estiverem desativados, o "npm install" termina sem erro mas o
# aplicativo nao abre ("Electron failed to install correctly").
ignore-scripts=false
