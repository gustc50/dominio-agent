{
  "name": "dominio-agent",
  "version": "0.1.0",
  "description": "Assistente de IA (Claude ou OpenRouter) para operar o sistema Domínio (Thomson Reuters) via API.",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "postinstall": "node node_modules/electron/install.js"
  },
  "devDependencies": {
    "electron": "^44.0.0"
  }
}
